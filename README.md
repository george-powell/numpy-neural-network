# numpy-neural-network
A neural network built from scratch using only NumPy (and Matplotlib for visualisation). It features arbitrary layer architectures and activation functions, backpropagation, mini-batch training and SGD, Momentum and Adam optimisation algorithms.
